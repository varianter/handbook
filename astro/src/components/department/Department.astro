---
// Department.astro — simplified stub (auth removed)
// In the original Next.js version, this gated content by user department.
// Since we're skipping auth, we render children unconditionally.
---
<slot />
